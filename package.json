{
  "name": "express-react",
  "version": "1.0.0",
  "description": "* `server.js`\r * `npm init` package.json 생성",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
